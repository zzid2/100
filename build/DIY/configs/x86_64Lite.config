
#------------------x86_64精简版 机型配置------------------
CONFIG_TARGET_x86=y                        ## x86平台
CONFIG_TARGET_x86_64=y                     ## 64位
CONFIG_TARGET_x86_64_DEVICE_generic=y      ## 通用设备

CONFIG_TARGET_ROOTFS_SQUASHFS=y            ## squashfs格式镜像（可恢复出厂设置）
CONFIG_GRUB_IMAGES=y                       ## Legacy 引导镜像
CONFIG_GRUB_EFI_IMAGES=n                   ## UEFI   引导镜像（需强制取消）
CONFIG_VMDK_IMAGES=y                       ## VMDK   镜像（vm虚拟机）
CONFIG_TARGET_IMAGES_GZIP=y                ## GZIP   压缩镜像
CONFIG_TARGET_ROOTFS_TARGZ=n               ## root根系统镜像（需强制取消）
